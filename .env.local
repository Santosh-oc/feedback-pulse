# Local testing only — matches docker-compose.yml.
PG_HOST=127.0.0.1
PG_PORT=5432
PG_USER=feedback
PG_PASSWORD=feedback
PG_DATABASE=feedback

MINIO_ENDPOINT=127.0.0.1:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=feedback-pulse
MINIO_SECURE=false

# Fallbacks used until LLM settings are saved on the Settings page.
LLM_BASE_URL=
LLM_TOKEN=
LLM_MODEL=
